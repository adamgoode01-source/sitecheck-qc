{
  "name": "capacitor-sitecheck-ar",
  "version": "0.1.0",
  "description": "ARKit depth measurement plugin for LiDAR Site Check.",
  "license": "UNLICENSED",
  "private": true,
  "//": "No `main`: this plugin is native-only. The app reaches it through registerPlugin('SiteCheckAR') from @capacitor/core, so there is no JS entry point to declare — and pointing at one that does not exist breaks plugin resolution.",
  "capacitor": {
    "ios": {
      "src": "ios"
    }
  },
  "files": [
    "ios/",
    "CapacitorSitecheckAr.podspec"
  ],
  "peerDependencies": {
    "@capacitor/core": "^6.0.0"
  }
}
